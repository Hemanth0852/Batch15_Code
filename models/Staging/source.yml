version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('SALES')}}"
      schema: RAWDATA
      tables:
        - name: Employees_Raw
        - name: Employee_Functions
        - name: raw_customerdata
        - name: raw_orders_data
